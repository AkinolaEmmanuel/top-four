# Auth + data currently run on an in-memory mock backend (lib/mock-db, lib/mock-auth)
# — no environment variables are required for it in dev.

# API-Football (https://www.api-football.com) — currently mocked in lib/api-football.
# Set this and swap lib/api-football/client.ts to a live fetch once you have a key.
API_FOOTBALL_KEY=your-api-football-key
 